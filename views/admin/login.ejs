<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Sendika Kullanıcı Girişi</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f7f7f7;
      margin: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    .logo-section {
      text-align: center;
      margin: 30px 0;
    }

    .logo-section img {
      width: 100px;
      height: auto;
    }

    .logo-section h1 {
      color: #b71c1c;
      font-size: 24px;
      margin-top: 10px;
    }

    .login-container {
      max-width: 400px;
      background: #fff;
      margin: 0 auto;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 15px rgba(0,0,0,0.2);
    }

    .login-container h2 {
      text-align: center;
      color: #b71c1c;
      margin-bottom: 20px;
    }

    .login-container label {
      font-weight: bold;
      font-size: 14px;
    }

    .login-container input {
      width: 100%;
      padding: 10px;
      margin: 6px 0 14px 0;
      border-radius: 7px;
      border: 1px solid #aaa;
      font-size: 15px;
    }

    .login-container button {
      width: 100%;
      padding: 12px;
      background: #d32f2f;
      color: #fff;
      border: none;
      border-radius: 7px;
      font-size: 17px;
      cursor: pointer;
      transition: 0.25s;
    }

    .login-container button:hover {
      background: #b71c1c;
    }

    .help {
      text-align: center;
      margin-top: 12px;
      font-size: 14px;
    }

    .help a {
      color: #d32f2f;
      text-decoration: none;
    }

    .error {
      text-align: center;
      color: #d32f2f;
      margin-bottom: 15px;
      font-weight: bold;
    }

    footer {
      margin-top: auto;
    }
  </style>
</head>
<body>

  <!-- NAV -->
  <%- include('../partials/nav') %>

  <!-- Logo ve Başlık -->
  <div class="logo-section">
    <img src="/static/img/1.png" alt="Sendika Logosu">
    <h1>SOSYAL HİZMETLER SENDİKASI KULLANICI GİRİŞİ</h1>
  </div>

  <!-- Kullanıcı Girişi Formu -->
  <div class="login-container">
    <h2>Kullanıcı Girişi</h2>

    <% if (typeof error !== "undefined") { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form action="/login" method="POST">
      <label for="username">Kullanıcı Adı</label>
      <input type="text" id="username" name="username" required placeholder="Kullanıcı adınızı girin">

      <label for="password">Şifre</label>
      <input type="password" id="password" name="password" required placeholder="Şifrenizi girin">

      <button type="submit">Giriş Yap</button>
    </form>

    <p class="help">Şifrenizi mi unuttunuz? <a href="#">Tıklayın</a></p>
  </div>

  <!-- Footer -->
  <footer class="w3-container w3-red w3-center w3-padding-32">
    <%- include("../partials/footer") %>
  </footer>

  <!-- Bootstrap JS (opsiyonel) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
